import React from "react";

const Aplicationpepper = ({ src, description }) => (
  <div className="pepper__apps-element">
    {src}
    <p>{description}</p>
  </div>
);

export default Aplicationpepper;
